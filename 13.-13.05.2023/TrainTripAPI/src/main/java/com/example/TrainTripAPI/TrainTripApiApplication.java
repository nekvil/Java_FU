package com.example.TrainTripAPI;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

@SpringBootApplication
public class TrainTripApiApplication {
	/**
	 * The main method is the entry point of the TrainTripApiApplication class.
	 * It launches the Train Trip API application.
	 *
	 * @param args the command line arguments
	 */
	public static void main(String[] args) {
		SpringApplication.run(TrainTripApiApplication.class, args);
	}
}
